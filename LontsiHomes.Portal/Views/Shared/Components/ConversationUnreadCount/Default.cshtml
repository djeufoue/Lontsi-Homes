@model int
<span class="rh-nav-unread-badge"
      data-conversation-unread-count
      aria-label="@T["{0} unread message(s)", Model]"
      hidden="@(Model <= 0 ? "hidden" : null)">@(Model > 99 ? "99+" : Model)</span>
